// Seed data functionality - currently disabled
// This file can be used to populate the database with initial data if needed

const seedData = async () => {
  console.log('🌱 Seed data is disabled - database will start clean');
  // No seeding will occur - database starts empty
  return;
};

module.exports = seedData;